a = 177
b = 10
# if we want to print quoient and remainder of a/b
print(divmod(a,b))
# One of the built-in functions of Python is divmod,
# which takes two arguments a and b and returns a tuple
# containing the quotient of a//b first and then the remainder a%b.