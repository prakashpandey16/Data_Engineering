import numpy as np  # Importing the NumPy library for working with arrays

# Creating an uninitialized 1D array with 5 elements
empty_array = np.empty(5)  # The array will have 5 elements with arbitrary values
print(empty_array)  # Printing the uninitialized array
